-- This builds a HTML file with text displaying `Hello, world`!

local Vyne = require 'vyne';
local element = Vyne:createElement("p", nil, "Hello, world");

Vyne
    :build(element)
    :mount();